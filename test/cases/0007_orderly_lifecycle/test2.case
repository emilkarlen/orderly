[setup]

env CLEAN_SHUTDOWN = no

[act]

orderly -max-start-tokens 2 -start-tokens-per-second 0 -all-lifecycle-hooks @[scripts]@/lifecycle -- -name sv -all-commands @[scripts]@/sv

[assert]

exit-code != 0

stdout
  -transformed-by grep ^sv|^lifecycle
    equals -contents-of test2.expected

[setup]

env CLEAN_SHUTDOWN = yes

[act]

orderly -max-start-tokens 1 -start-tokens-per-second 0 -all-lifecycle-hooks @[scripts]@/lifecycle -- -name sv -all-commands @[scripts]@/sv

[assert]

exit-code == 0

stdout
  -transformed-by grep ^sv|^lifecycle
    equals -contents-of test1.expected
